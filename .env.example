TARGET_URL=https://example.com
AUTH_USERNAME=changeme
AUTH_PASSWORD=changeme
CONTEXT_CHARS=80
CONCURRENCY=4
# Both unset by default: the crawl runs until the frontier actually
# empties, not a guessed page count. Uncomment either for an explicit
# opt-in safety ceiling.
# MAX_PAGES=1000
# MAX_DURATION_SECONDS=1800
